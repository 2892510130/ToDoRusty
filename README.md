# ToDoRusty
A todo list terminal exe in rust.

# Tips
- Create a profile is there is none: New-Item -Path $PROFILE -Type File -Force
- Profile is in ~/Documents/WindowsPowerShell/Microsoft.PowerShell_profile.ps1
- In the profile add & "/path/app.exe"
